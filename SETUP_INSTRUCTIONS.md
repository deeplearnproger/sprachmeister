# AITalkingApp - Setup Instructions

## Обзор

Приложение для практики устной части экзамена Goethe B1 полностью реализовано!

## Структура проекта

```
AITalkingApp/
├── Models/                     # Модели данных
│   ├── Scenario.swift          # Сценарии экзамена (Bildbeschreibung, etc.)
│   ├── Transcript.swift        # Транскрипты разговоров
│   ├── Metrics.swift           # Метрики обратной связи
│   ├── Attempt.swift           # Попытки практики
│   └── ConversationState.swift # Состояния беседы
├── Services/                   # Сервисный слой
│   ├── AudioService.swift      # Запись аудио + VAD
│   ├── STTService.swift        # Распознавание речи (SFSpeech)
│   ├── TTSService.swift        # Синтез речи (AVSpeech)
│   ├── ResponseEngine.swift    # Генерация ответов экзаменатора
│   └── StorageService.swift    # Локальное хранилище
├── Orchestration/              # Управление потоком
│   └── ConversationOrchestrator.swift # Главный оркестратор
├── Views/                      # UI компоненты
│   ├── ScenarioPicker.swift    # Выбор сценария
│   ├── PracticeScreen.swift    # Экран практики
│   ├── TranscriptScreen.swift  # Просмотр транскрипта
│   └── AttemptsHistoryView.swift # История попыток
└── Utils/                      # Утилиты
    ├── MetricsAnalyzer.swift   # Анализ метрик
    └── VAD.swift               # Voice Activity Detection
```

## Шаги по настройке

### 1. Добавление файлов в Xcode проект

**ВАЖНО:** Все исходные файлы уже созданы, но их нужно добавить в проект Xcode.

#### Способ 1: Добавление через Xcode (Рекомендуется)

1. Откройте `AITalkingApp.xcodeproj` в Xcode
2. В Project Navigator (левая панель), щелкните правой кнопкой на папку `AITalkingApp`
3. Выберите **"Add Files to AITalkingApp..."**
4. Перейдите в папку `/Users/t.abkiliamov/Documents/deutsch app/AITalkingApp/AITalkingApp/`
5. Выберите следующие папки (с галочкой "Create groups"):
   - `Models`
   - `Services`
   - `Orchestration`
   - `Views`
   - `Utils`
6. Также добавьте файл `Info.plist`
7. Убедитесь, что выбрана опция **"Copy items if needed"** (не обязательно, файлы уже в нужном месте)
8. Нажмите **"Add"**

#### Способ 2: Перетаскивание

1. Откройте Finder и перейдите в `/Users/t.abkiliamov/Documents/deutsch app/AITalkingApp/AITalkingApp/`
2. Откройте Xcode с проектом
3. Перетащите папки `Models`, `Services`, `Orchestration`, `Views`, `Utils` в Project Navigator
4. Выберите "Create groups" и убедитесь, что target `AITalkingApp` выбран

### 2. Настройка Info.plist

Info.plist уже создан с необходимыми разрешениями:
- ✅ Microphone Usage Description (NSMicrophoneUsageDescription)
- ✅ Speech Recognition Usage Description (NSSpeechRecognitionUsageDescription)
- ✅ Background Modes (audio)

Убедитесь, что этот файл выбран в Target → Build Settings → Info.plist File

### 3. Проверка сборки

1. Выберите целевое устройство (iPhone 16 или симулятор)
2. Нажмите **Cmd + B** для сборки
3. Исправьте любые ошибки импорта или зависимостей

### 4. Запуск на устройстве

#### Для установки через Xcode (Free Provisioning):

1. Подключите iPhone через USB
2. В Xcode: **Signing & Capabilities** → выберите свой Apple ID team
3. Нажмите **Cmd + R** для запуска
4. На iPhone: **Settings → General → VPN & Device Management** → доверьте сертификату
5. Приложение установлено! (Действительно 7 дней, затем нужно пересобрать)

## Функциональность

### ✅ Реализовано

1. **Модели данных**
   - Сценарии (3 типа: Bildbeschreibung, Mini-Präsentation, Dialog)
   - Транскрипты разговоров
   - Метрики производительности
   - История попыток

2. **Сервисы**
   - **AudioService**: Запись с микрофона + автоматическое определение речи (VAD)
   - **STTService**: Распознавание немецкой речи (on-device через SFSpeech)
   - **TTSService**: Синтез немецкой речи (AVSpeechSynthesizer)
   - **ResponseEngine**: Шаблонные ответы экзаменатора на немецком
   - **StorageService**: Локальное сохранение попыток (JSON)

3. **Анализ метрик**
   - Подсчет слов в минуту (WPM)
   - Обнаружение филлеров (äh, ähm, etc.)
   - Лексическое разнообразие (Type-Token Ratio)
   - Базовая проверка грамматики (артикли, позиция глагола)
   - Оценка производительности (Ausgezeichnet/Gut/Befriedigend/Verbesserungsbedarf)

4. **UI**
   - **ScenarioPicker**: Выбор сценария по типам
   - **PracticeScreen**: Интерактивная практика с визуальной обратной связью
   - **TranscriptScreen**: Просмотр полного транскрипта с временными метками
   - **AttemptsHistoryView**: История с фильтрацией и статистикой

5. **Оркестрация**
   - State machine: Idle → Ready → Recording → Transcribing → Processing → Speaking → Waiting
   - Автоматическая смена ролей (экзаменатор ↔ ученик)
   - Завершение сессии с обратной связью

### 🎯 Характеристики MVP

- ✅ Offline-first (все обработка на устройстве)
- ✅ Free provisioning (без платного Apple Developer)
- ✅ iOS 16.0+ совместимость
- ✅ 3 типа сценариев экзамена B1
- ✅ Автоматическое распознавание речи
- ✅ Немецкая TTS обратная связь
- ✅ Метрики и аналитика
- ✅ Локальная история попыток

## Известные ограничения

### STT

**Важно:** Приложение использует `SFSpeechRecognizer` вместо Whisper для MVP, так как:
- Whisper требует дополнительные зависимости (WhisperKit или whisper.cpp)
- SFSpeech работает on-device для немецкого языка на iOS 17+
- Для полной offline работы с Whisper нужно добавить модель (~100-500MB)

**Для добавления Whisper в будущем:**
1. Добавьте Swift Package: `https://github.com/argmaxinc/WhisperKit`
2. Загрузите модель `whisper-small-de` или `whisper-base-de`
3. Обновите `STTService` для использования WhisperKit

### TTS

- Используется системный немецкий голос (Anna-compact)
- Требуется загрузка немецкого голоса в **Settings → Accessibility → Spoken Content → Voices**

## Тестирование

### Перед запуском на устройстве:

1. Убедитесь, что микрофон работает
2. Загрузите немецкий голос в настройках iOS
3. Проверьте права доступа к микрофону и распознаванию речи

### Тестовые сценарии:

1. **Bildbeschreibung**
   - Выберите "Familienfoto"
   - Опишите воображаемое семейное фото по-немецки
   - Ответьте на вопросы экзаменатора

2. **Mini-Präsentation**
   - Выберите "Mein Lieblingshobby"
   - Расскажите о своем хобби (1-2 минуты)
   - Отвечайте на уточняющие вопросы

3. **Dialog/Planung**
   - Выберите "Eine Geburtstagsparty planen"
   - Обсудите планирование вечеринки
   - Согласуйте детали с "экзаменатором"

## Следующие шаги (Post-MVP)

1. **Интеграция Whisper** для полного offline режима
2. **Расширенная грамматика** через NLP модели
3. **Визуальные подсказки** (картинки для Bildbeschreibung)
4. **Экспорт данных** (PDF отчеты)
5. **Темная тема**
6. **Локализация UI** (полностью на немецком)
7. **iCloud синхронизация** истории

## Устранение неполадок

### Ошибка: "Microphone permission denied"
→ Перейдите в **Settings → Privacy → Microphone** и разрешите доступ

### Ошибка: "Speech recognition unavailable"
→ Проверьте **Settings → Privacy → Speech Recognition**

### Голос не говорит
→ Загрузите немецкий голос: **Settings → Accessibility → Spoken Content → Voices → German**

### Приложение перестало работать через 7 дней
→ Пересоберите и переустановите через Xcode (ограничение free provisioning)

## Контакты

Разработано для практики Goethe B1 Zertifikat Deutsch
Создано: 20.10.2025

---

**Viel Erfolg bei der Prüfung! 🇩🇪**
